import { Cart } from '../../../features/products/cart';
import { Header } from '../../components/header';

const CartPage = () => {
  return (
    <div>
      <Header />
      <Cart />
    </div>
  );
};

export { CartPage };
